server:
  port: 8089

spring:
  application:
    name: wjweb
  cloud:
    nacos:
#      username: nacos
#      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
#        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        #指定分组
        group: DEFAULT_GROUP
        #指定命名空间
#        namespace: public
